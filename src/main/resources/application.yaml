spring:
  application:
    name: picsou
  datasource:
    url: jdbc:h2:mem:picsou-db
    username: bd-username
    password: bd-password
    driverClassName: org.h2.Driver
    jpa:
      hibernate:
        ddl-auto: update

server:
  port: 8888